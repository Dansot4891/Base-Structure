import 'package:flutter/material.dart';

class WowBoardPage extends StatelessWidget {
  const WowBoardPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('WowBoardPage'),
      ),
    );
  }
}
